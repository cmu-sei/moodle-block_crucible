{{!
  @template block_crucible/landing
}}
<div class="crucible-card crucible-apps">
  <!-- Header -->
  <div class="crucible-card__header">
    <div class="crucible-card__icon">🧩</div>
    <div class="crucible-card__titlewrap">
      <div class="crucible-card__title">{{cardtitle}}</div>

      {{#welcomemessage}}
        <div class="crucible-card__subtitle">{{welcomemessage}}</div>
      {{/welcomemessage}}
      {{^welcomemessage}}
        <div class="crucible-card__subtitle">
          {{#str}} appswelcome, block_crucible {{/str}}
          <strong>{{username}}</strong> · {{sitename}}
        </div>
      {{/welcomemessage}}
    </div>
  </div>

  <!-- Body -->
  <div class="crucible-card__body">
    <div class="apps-grid">

      {{#orderedapps}}
        <div class="app-card" id="{{key}}">
          <div class="app-card__content">
            <div class="app-card__details">
              <div class="app-card__title">{{name}}</div>
              <div class="app-card__desc">{{{description}}}</div>
            </div>
            <div class="app-card__link">
              <a href="{{url}}" target="_blank">
                <img class="app-card__logo" src="{{logo}}" alt="{{name}}">
              </a>
            </div>
          </div>
        </div>
      {{/orderedapps}}

    </div>
  </div>
</div>

{{#hasdragdrop}}
  {{#js}}
    require(['block_crucible/apporder'], function(AppOrder) {
      AppOrder.init('.apps-grid');
    });
  {{/js}}
{{/hasdragdrop}}
